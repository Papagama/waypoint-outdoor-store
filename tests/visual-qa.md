# Visual QA — 2026-08-31

## Status: blocked by environment

The production build completed, and local HTTP routes return 200. The required rendered desktop/mobile inspection could not be performed: browser-control runtime exited during sandbox setup before it could open the local page.

## Required retest when preview is available

1. Desktop: hero contrast, catalog grid, filter rail, product gallery, drawer cart, checkout form and footer.
2. Mobile: header, hero crop, category list, two-column cards, filter sheet, sticky product action, kit answers, cart and checkout.
3. Keyboard: menu, search, filters, color selections, quantity controls and checkout errors.
4. Reduced motion: hover/reveal behaviour remains non-essential.
